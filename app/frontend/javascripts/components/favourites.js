import Axios from 'axios';

(function (document, window, Orn, Utils) {
  "use strict";

  var Favourites = {
    setFavourites = () => {
      
    },
    
    init: function() {

    }
  }
  
  Orn.registerComponent("Favourites", Favourites);

}(document, window, Ornament, Ornament.Utilities));